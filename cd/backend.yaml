apiVersion: apps/v1
kind: Deployment
metadata:
  name: library-backend
  namespace: library
spec:
  replicas: 1
  selector:
    matchLabels:
      app: library-backend
  template:
    metadata:
      labels:
        app: library-backend
    spec:
      containers:
        - name: library-backend
          image: wngud1/backend-library:wngud1-backend-library-main-3dc35e7cc9d747210024adc57505c4d0aafcc0cc
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: TZ
              value: Asia/Seoul
            - name: SPRING_DATA_MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: mongodb-secret
                  key: SPRING_DATA_MONGODB_URI
            - name: SPRING_DATA_REDIS_HOST
              value: redis-service.default.svc.cluster.local
            - name: SPRING_DATA_REDIS_PORT
              value: '6379'
